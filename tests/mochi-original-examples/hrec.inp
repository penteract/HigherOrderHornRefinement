environment
f : (int->int->bool)->int->bool
succ : int->int->bool;


program

r=x+1 => succ x r
x>=0 ^ g x r ⇒ f g x r
x<0 ^ E y:int.g x y ^ f ??? y r ⇒ f g x r

/*
let rec f g x = if x>=0 then g x else f (f g) (g x)
let succ x = x+1
let main n = assert (f succ n >= 0)*/

goal
∃ m,n,r:int. m>=0 ^ n>=0 ^ ack m n r ^ r < n